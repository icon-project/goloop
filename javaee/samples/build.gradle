dependencies {
    implementation project(':api')
}

sourceSets {
    main.java.srcDirs = [
        'collection/src',
        'crowdsale/src',
        'multisig/src',
        'sampletoken/src',
        'testcases/src',
    ]
}

compileJava {
    options.compilerArgs += ['-parameters']
}
